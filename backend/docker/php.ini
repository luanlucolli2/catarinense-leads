; backend/docker/php.ini

; 🔧 Sem limites de execução/memória para exportações pesadas
max_execution_time = 0
memory_limit       = -1

; Upload/POST
upload_max_filesize = 128M
post_max_size       = 128M

; Evita timeouts durante upload/processamento (para SAPIs que respeitam)
max_input_time  = 300
